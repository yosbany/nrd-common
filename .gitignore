node_modules/
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Note: dist/ is NOT ignored because we need to commit nrd-common.js for CDN access
# Only ignore dist contents during development, but commit the built file
